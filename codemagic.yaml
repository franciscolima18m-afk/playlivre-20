workflows:
  flutter:
    name: Flutter Build

    environment:
      flutter: stable

    scripts:
      - name: Criar plataformas Flutter
        script: |
          flutter create .

      - name: Instalar dependências
        script: |
          flutter pub get

      - name: Compilar APK
        script: |
          flutter build apk --release
